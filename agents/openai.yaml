interface:
  display_name: "Present as HTML"
  short_description: "Turn text into a polished visual webpage"
  default_prompt: "Use $present-as-html to turn this text into a beautiful, responsive HTML page."
